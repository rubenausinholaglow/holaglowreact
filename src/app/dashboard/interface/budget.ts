export interface Budget {
  UserId: string;
  StatusBudget: number;
  DiscountCode: string;
  DiscountAmount: string;
  TotalPrice: number;
  ClinicInfoId: string;
  ReferenceId: string;
}

export interface BudgetProduct {
  test: string;
}
